#!/usr/bin/env python3
"""
⚡ RunZero — Local GitHub Actions Runner Autoscaler
Dual-Engine Fleet supporting ultra-fast Docker containers and dedicated Virtual Machines
(OrbStack macOS Linux Machines, Windows WSL2, Canonical Multipass).
Includes persistent multi-language package caching, proxy registries, and adaptive rate-limiting.
"""

import os
import sys
import time
import signal
import json
import urllib.request
import urllib.error
from datetime import datetime, timezone, timedelta
from typing import List, Dict, Tuple, Any

from drivers import get_driver, get_available_drivers, RunnerDriver, RunnerInfo

# Configuration from environment variables
ACCESS_TOKEN = os.getenv("ACCESS_TOKEN") or os.getenv("GITHUB_TOKEN")
OWNER = os.getenv("OWNER", "").strip()
ORG = os.getenv("ORG", "").strip()
REPOS_CONFIG = os.getenv("REPOS") or os.getenv("REPO", "")
AUTO_DISCOVER = os.getenv("AUTO_DISCOVER_REPOS", "true").lower() in ("true", "1", "yes")
ACTIVE_DAYS = int(os.getenv("ACTIVE_REPO_DAYS", "60"))
DISCOVERY_INTERVAL = int(os.getenv("DISCOVERY_INTERVAL", "900"))

# Driver & Architecture configuration
RUNNER_BACKEND = os.getenv("RUNNER_BACKEND", "auto").lower().strip()
RUNNER_ARCH = os.getenv("RUNNER_ARCH", "both").lower().strip()
RUNNER_LABELS_CUSTOM = os.getenv("RUNNER_LABELS", "").strip()

# Hybrid Auto-Routing settings
AUTO_ROUTE_VM = os.getenv("AUTO_ROUTE_VM", "true").lower() in ("true", "1", "yes")
VM_TRIGGER_LABELS = [
    label.strip().lower()
    for label in os.getenv("VM_TRIGGER_LABELS", "vm,browser,e2e,lighthouse,systemd,gui,unconfined").split(",")
    if label.strip()
]

MIN_RUNNERS = int(os.getenv("MIN_RUNNERS", "0"))
MAX_RUNNERS = int(os.getenv("MAX_RUNNERS", "4"))
POLL_INTERVAL = int(os.getenv("POLL_INTERVAL", "10"))
DOCKER_SOCK = os.getenv("DOCKER_SOCK", "/var/run/docker.sock")
DOCKER_NETWORK = os.getenv("DOCKER_NETWORK", "host")

# Persistent Cache & Proxy Configuration
CACHE_ENABLED = os.getenv("CACHE_ENABLED", "true").lower() in ("true", "1", "yes")
PROXIES_ENABLED = os.getenv("PROXIES_ENABLED", "true").lower() in ("true", "1", "yes")
HOST_CACHE_DIR = os.getenv("HOST_CACHE_DIR") or os.path.expanduser("~/.local-github-runner/cache")

API_BASE = "https://api.github.com"
running = True

# Rate-limiting state
rate_limit_remaining = 5000
rate_limit_reset = time.time() + 3600


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_signal_handler__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_signal_handler__mutmut)
def signal_handler(signum, frame):
    global running
    print("\n[Autoscaler] Received shutdown signal. Cleaning up...")
    running = False


def x_signal_handler__mutmut_orig(signum, frame):
    global running
    print("\n[Autoscaler] Received shutdown signal. Cleaning up...")
    running = False


def x_signal_handler__mutmut_1(signum, frame):
    global running
    print(None)
    running = False


def x_signal_handler__mutmut_2(signum, frame):
    global running
    print("XX\n[Autoscaler] Received shutdown signal. Cleaning up...XX")
    running = False


def x_signal_handler__mutmut_3(signum, frame):
    global running
    print("\n[autoscaler] received shutdown signal. cleaning up...")
    running = False


def x_signal_handler__mutmut_4(signum, frame):
    global running
    print("\n[AUTOSCALER] RECEIVED SHUTDOWN SIGNAL. CLEANING UP...")
    running = False


def x_signal_handler__mutmut_5(signum, frame):
    global running
    print("\n[Autoscaler] Received shutdown signal. Cleaning up...")
    running = None


def x_signal_handler__mutmut_6(signum, frame):
    global running
    print("\n[Autoscaler] Received shutdown signal. Cleaning up...")
    running = True

mutants_x_signal_handler__mutmut['_mutmut_orig'] = x_signal_handler__mutmut_orig # type: ignore # mutmut generated
mutants_x_signal_handler__mutmut['x_signal_handler__mutmut_1'] = x_signal_handler__mutmut_1 # type: ignore # mutmut generated
mutants_x_signal_handler__mutmut['x_signal_handler__mutmut_2'] = x_signal_handler__mutmut_2 # type: ignore # mutmut generated
mutants_x_signal_handler__mutmut['x_signal_handler__mutmut_3'] = x_signal_handler__mutmut_3 # type: ignore # mutmut generated
mutants_x_signal_handler__mutmut['x_signal_handler__mutmut_4'] = x_signal_handler__mutmut_4 # type: ignore # mutmut generated
mutants_x_signal_handler__mutmut['x_signal_handler__mutmut_5'] = x_signal_handler__mutmut_5 # type: ignore # mutmut generated
mutants_x_signal_handler__mutmut['x_signal_handler__mutmut_6'] = x_signal_handler__mutmut_6 # type: ignore # mutmut generated


signal.signal(signal.SIGINT, signal_handler)
signal.signal(signal.SIGTERM, signal_handler)
mutants_x_init_cache_dirs__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_init_cache_dirs__mutmut)
def init_cache_dirs():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_orig():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_1():
    """Ensure host cache directories exist."""
    if CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_2():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = None

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_3():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "XXtoolcacheXX": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_4():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "TOOLCACHE": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_5():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "XX/opt/hostedtoolcacheXX",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_6():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/OPT/HOSTEDTOOLCACHE",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_7():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "XXnpmXX": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_8():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "NPM": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_9():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "XX/home/runner/.npmXX",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_10():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/HOME/RUNNER/.NPM",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_11():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "XXyarnXX": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_12():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "YARN": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_13():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "XX/home/runner/.cache/yarnXX",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_14():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/HOME/RUNNER/.CACHE/YARN",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_15():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "XXpnpmXX": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_16():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "PNPM": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_17():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "XX/home/runner/.local/share/pnpm/storeXX",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_18():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/HOME/RUNNER/.LOCAL/SHARE/PNPM/STORE",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_19():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "XXpipXX": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_20():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "PIP": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_21():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "XX/home/runner/.cache/pipXX",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_22():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/HOME/RUNNER/.CACHE/PIP",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_23():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "XXuvXX": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_24():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "UV": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_25():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "XX/home/runner/.cache/uvXX",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_26():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/HOME/RUNNER/.CACHE/UV",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_27():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "XXgo-modXX": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_28():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "GO-MOD": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_29():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "XX/home/runner/go/pkg/modXX",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_30():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/HOME/RUNNER/GO/PKG/MOD",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_31():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "XXgo-buildXX": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_32():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "GO-BUILD": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_33():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "XX/home/runner/.cache/go-buildXX",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_34():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/HOME/RUNNER/.CACHE/GO-BUILD",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_35():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "XXcargo-registryXX": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_36():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "CARGO-REGISTRY": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_37():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "XX/home/runner/.cargo/registryXX"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_38():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/HOME/RUNNER/.CARGO/REGISTRY"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_39():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(None, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_40():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=None)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_41():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_42():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, )
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_43():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=False)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_44():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = None
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_45():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = None
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_46():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(None, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_47():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, None)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_48():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_49():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, )
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_50():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(None, exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_51():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=None)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_52():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(exist_ok=True)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_53():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, )
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_54():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=False)
        mount_mappings[host_path] = container_path

    return mount_mappings


def x_init_cache_dirs__mutmut_55():
    """Ensure host cache directories exist."""
    if not CACHE_ENABLED:
        return {}

    subdirs = {
        "toolcache": "/opt/hostedtoolcache",
        "npm": "/home/runner/.npm",
        "yarn": "/home/runner/.cache/yarn",
        "pnpm": "/home/runner/.local/share/pnpm/store",
        "pip": "/home/runner/.cache/pip",
        "uv": "/home/runner/.cache/uv",
        "go-mod": "/home/runner/go/pkg/mod",
        "go-build": "/home/runner/.cache/go-build",
        "cargo-registry": "/home/runner/.cargo/registry"
    }

    os.makedirs(HOST_CACHE_DIR, exist_ok=True)
    mount_mappings = {}
    for key, container_path in subdirs.items():
        host_path = os.path.join(HOST_CACHE_DIR, key)
        os.makedirs(host_path, exist_ok=True)
        mount_mappings[host_path] = None

    return mount_mappings

mutants_x_init_cache_dirs__mutmut['_mutmut_orig'] = x_init_cache_dirs__mutmut_orig # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_1'] = x_init_cache_dirs__mutmut_1 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_2'] = x_init_cache_dirs__mutmut_2 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_3'] = x_init_cache_dirs__mutmut_3 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_4'] = x_init_cache_dirs__mutmut_4 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_5'] = x_init_cache_dirs__mutmut_5 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_6'] = x_init_cache_dirs__mutmut_6 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_7'] = x_init_cache_dirs__mutmut_7 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_8'] = x_init_cache_dirs__mutmut_8 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_9'] = x_init_cache_dirs__mutmut_9 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_10'] = x_init_cache_dirs__mutmut_10 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_11'] = x_init_cache_dirs__mutmut_11 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_12'] = x_init_cache_dirs__mutmut_12 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_13'] = x_init_cache_dirs__mutmut_13 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_14'] = x_init_cache_dirs__mutmut_14 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_15'] = x_init_cache_dirs__mutmut_15 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_16'] = x_init_cache_dirs__mutmut_16 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_17'] = x_init_cache_dirs__mutmut_17 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_18'] = x_init_cache_dirs__mutmut_18 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_19'] = x_init_cache_dirs__mutmut_19 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_20'] = x_init_cache_dirs__mutmut_20 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_21'] = x_init_cache_dirs__mutmut_21 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_22'] = x_init_cache_dirs__mutmut_22 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_23'] = x_init_cache_dirs__mutmut_23 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_24'] = x_init_cache_dirs__mutmut_24 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_25'] = x_init_cache_dirs__mutmut_25 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_26'] = x_init_cache_dirs__mutmut_26 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_27'] = x_init_cache_dirs__mutmut_27 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_28'] = x_init_cache_dirs__mutmut_28 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_29'] = x_init_cache_dirs__mutmut_29 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_30'] = x_init_cache_dirs__mutmut_30 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_31'] = x_init_cache_dirs__mutmut_31 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_32'] = x_init_cache_dirs__mutmut_32 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_33'] = x_init_cache_dirs__mutmut_33 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_34'] = x_init_cache_dirs__mutmut_34 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_35'] = x_init_cache_dirs__mutmut_35 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_36'] = x_init_cache_dirs__mutmut_36 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_37'] = x_init_cache_dirs__mutmut_37 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_38'] = x_init_cache_dirs__mutmut_38 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_39'] = x_init_cache_dirs__mutmut_39 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_40'] = x_init_cache_dirs__mutmut_40 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_41'] = x_init_cache_dirs__mutmut_41 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_42'] = x_init_cache_dirs__mutmut_42 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_43'] = x_init_cache_dirs__mutmut_43 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_44'] = x_init_cache_dirs__mutmut_44 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_45'] = x_init_cache_dirs__mutmut_45 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_46'] = x_init_cache_dirs__mutmut_46 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_47'] = x_init_cache_dirs__mutmut_47 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_48'] = x_init_cache_dirs__mutmut_48 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_49'] = x_init_cache_dirs__mutmut_49 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_50'] = x_init_cache_dirs__mutmut_50 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_51'] = x_init_cache_dirs__mutmut_51 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_52'] = x_init_cache_dirs__mutmut_52 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_53'] = x_init_cache_dirs__mutmut_53 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_54'] = x_init_cache_dirs__mutmut_54 # type: ignore # mutmut generated
mutants_x_init_cache_dirs__mutmut['x_init_cache_dirs__mutmut_55'] = x_init_cache_dirs__mutmut_55 # type: ignore # mutmut generated
mutants_x_github_request__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_github_request__mutmut)
def github_request(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_orig(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_1(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = None
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_2(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 or now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_3(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining <= 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_4(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 16 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_5(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now <= rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_6(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = None
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_7(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(None, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_8(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, None)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_9(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_10(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, )
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_11(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(6, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_12(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) - 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_13(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(None) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_14(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset + now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_15(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 3)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_16(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(None, file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_17(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=None)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_18(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_19(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", )
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_20(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(None)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_21(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = None
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_22(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = None
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_23(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(None)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_24(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header(None, f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_25(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", None)
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_26(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header(f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_27(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", )
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_28(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("XXAuthorizationXX", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_29(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_30(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("AUTHORIZATION", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_31(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header(None, "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_32(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", None)
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_33(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_34(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", )
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_35(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("XXAcceptXX", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_36(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_37(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("ACCEPT", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_38(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "XXapplication/vnd.github+jsonXX")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_39(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "APPLICATION/VND.GITHUB+JSON")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_40(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header(None, "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_41(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", None)
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_42(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_43(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", )
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_44(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("XXX-GitHub-Api-VersionXX", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_45(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("x-github-api-version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_46(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GITHUB-API-VERSION", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_47(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "XX2022-11-28XX")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_48(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header(None, "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_49(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", None)

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_50(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_51(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", )

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_52(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("XXUser-AgentXX", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_53(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("user-agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_54(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("USER-AGENT", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_55(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "XXRunZero-AutoscalerXX")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_56(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "runzero-autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_57(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RUNZERO-AUTOSCALER")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_58(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(None, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_59(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=None) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_60(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_61(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, ) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_62(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=11) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_63(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = None
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_64(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "XXx-ratelimit-remainingXX" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_65(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "X-RATELIMIT-REMAINING" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_66(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" not in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_67(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = None
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_68(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(None)
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_69(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["XXx-ratelimit-remainingXX"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_70(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["X-RATELIMIT-REMAINING"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_71(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = None
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_72(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(None)
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_73(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["XXx-ratelimit-resetXX"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_74(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["X-RATELIMIT-RESET"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_75(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(None)
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_76(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode(None))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_77(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("XXutf-8XX"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_78(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("UTF-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_79(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = None
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_80(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers and {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_81(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "XXx-ratelimit-remainingXX" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_82(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "X-RATELIMIT-REMAINING" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_83(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" not in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_84(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = None
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_85(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(None)
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_86(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["XXx-ratelimit-remainingXX"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_87(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["X-RATELIMIT-REMAINING"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_88(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = None
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_89(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(None)
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_90(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["XXx-ratelimit-resetXX"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_91(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["X-RATELIMIT-RESET"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_92(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code not in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_93(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (404, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_94(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 430):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_95(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = None
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_96(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(None, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_97(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, None) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_98(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_99(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, ) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_100(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(11, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_101(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) - 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_102(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(None) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_103(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset + time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_104(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 3) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_105(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset >= time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_106(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 61
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_107(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(None, file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_108(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=None)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_109(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_110(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", )
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_111(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(None)
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_112(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(None, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_113(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, None))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_114(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_115(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, ))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_116(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 301))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_117(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = None
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_118(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode(None) if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_119(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("XXutf-8XX") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_120(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("UTF-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_121(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else "XXXX"
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_122(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(None, file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_123(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=None)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_124(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_125(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", )
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=sys.stderr)
        return None


def x_github_request__mutmut_126(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(None, file=sys.stderr)
        return None


def x_github_request__mutmut_127(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", file=None)
        return None


def x_github_request__mutmut_128(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(file=sys.stderr)
        return None


def x_github_request__mutmut_129(endpoint):
    """Perform authenticated GitHub REST API GET request with rate-limit tracking."""
    global rate_limit_remaining, rate_limit_reset

    now = time.time()
    if rate_limit_remaining < 15 and now < rate_limit_reset:
        wait_seconds = max(5, int(rate_limit_reset - now) + 2)
        print(f"[Autoscaler] ⚠️ API rate limit low ({rate_limit_remaining} remaining). Backing off for {wait_seconds}s...", file=sys.stderr)
        time.sleep(wait_seconds)

    url = f"{API_BASE}{endpoint}"
    req = urllib.request.Request(url)
    req.add_header("Authorization", f"Bearer {ACCESS_TOKEN}")
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("X-GitHub-Api-Version", "2022-11-28")
    req.add_header("User-Agent", "RunZero-Autoscaler")

    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            headers = resp.headers
            if "x-ratelimit-remaining" in headers:
                try:
                    rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                    rate_limit_reset = int(headers["x-ratelimit-reset"])
                except Exception:
                    pass
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        headers = e.headers or {}
        if "x-ratelimit-remaining" in headers:
            try:
                rate_limit_remaining = int(headers["x-ratelimit-remaining"])
                rate_limit_reset = int(headers["x-ratelimit-reset"])
            except Exception:
                pass

        if e.code in (403, 429):
            wait_seconds = max(10, int(rate_limit_reset - time.time()) + 2) if rate_limit_reset > time.time() else 60
            print(f"[Autoscaler] ⛔ GitHub API rate limit reached ({e.code}). Pausing for {wait_seconds}s...", file=sys.stderr)
            time.sleep(min(wait_seconds, 300))
        else:
            body = e.read().decode("utf-8") if e.fp else ""
            print(f"[Autoscaler] API Error ({e.code}) for {endpoint}: {body}", file=sys.stderr)
        return None
    except Exception as e:
        print(f"[Autoscaler] Request Error for {endpoint}: {e}", )
        return None

mutants_x_github_request__mutmut['_mutmut_orig'] = x_github_request__mutmut_orig # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_1'] = x_github_request__mutmut_1 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_2'] = x_github_request__mutmut_2 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_3'] = x_github_request__mutmut_3 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_4'] = x_github_request__mutmut_4 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_5'] = x_github_request__mutmut_5 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_6'] = x_github_request__mutmut_6 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_7'] = x_github_request__mutmut_7 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_8'] = x_github_request__mutmut_8 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_9'] = x_github_request__mutmut_9 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_10'] = x_github_request__mutmut_10 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_11'] = x_github_request__mutmut_11 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_12'] = x_github_request__mutmut_12 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_13'] = x_github_request__mutmut_13 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_14'] = x_github_request__mutmut_14 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_15'] = x_github_request__mutmut_15 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_16'] = x_github_request__mutmut_16 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_17'] = x_github_request__mutmut_17 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_18'] = x_github_request__mutmut_18 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_19'] = x_github_request__mutmut_19 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_20'] = x_github_request__mutmut_20 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_21'] = x_github_request__mutmut_21 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_22'] = x_github_request__mutmut_22 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_23'] = x_github_request__mutmut_23 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_24'] = x_github_request__mutmut_24 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_25'] = x_github_request__mutmut_25 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_26'] = x_github_request__mutmut_26 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_27'] = x_github_request__mutmut_27 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_28'] = x_github_request__mutmut_28 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_29'] = x_github_request__mutmut_29 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_30'] = x_github_request__mutmut_30 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_31'] = x_github_request__mutmut_31 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_32'] = x_github_request__mutmut_32 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_33'] = x_github_request__mutmut_33 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_34'] = x_github_request__mutmut_34 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_35'] = x_github_request__mutmut_35 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_36'] = x_github_request__mutmut_36 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_37'] = x_github_request__mutmut_37 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_38'] = x_github_request__mutmut_38 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_39'] = x_github_request__mutmut_39 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_40'] = x_github_request__mutmut_40 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_41'] = x_github_request__mutmut_41 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_42'] = x_github_request__mutmut_42 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_43'] = x_github_request__mutmut_43 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_44'] = x_github_request__mutmut_44 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_45'] = x_github_request__mutmut_45 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_46'] = x_github_request__mutmut_46 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_47'] = x_github_request__mutmut_47 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_48'] = x_github_request__mutmut_48 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_49'] = x_github_request__mutmut_49 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_50'] = x_github_request__mutmut_50 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_51'] = x_github_request__mutmut_51 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_52'] = x_github_request__mutmut_52 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_53'] = x_github_request__mutmut_53 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_54'] = x_github_request__mutmut_54 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_55'] = x_github_request__mutmut_55 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_56'] = x_github_request__mutmut_56 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_57'] = x_github_request__mutmut_57 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_58'] = x_github_request__mutmut_58 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_59'] = x_github_request__mutmut_59 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_60'] = x_github_request__mutmut_60 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_61'] = x_github_request__mutmut_61 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_62'] = x_github_request__mutmut_62 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_63'] = x_github_request__mutmut_63 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_64'] = x_github_request__mutmut_64 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_65'] = x_github_request__mutmut_65 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_66'] = x_github_request__mutmut_66 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_67'] = x_github_request__mutmut_67 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_68'] = x_github_request__mutmut_68 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_69'] = x_github_request__mutmut_69 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_70'] = x_github_request__mutmut_70 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_71'] = x_github_request__mutmut_71 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_72'] = x_github_request__mutmut_72 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_73'] = x_github_request__mutmut_73 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_74'] = x_github_request__mutmut_74 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_75'] = x_github_request__mutmut_75 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_76'] = x_github_request__mutmut_76 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_77'] = x_github_request__mutmut_77 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_78'] = x_github_request__mutmut_78 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_79'] = x_github_request__mutmut_79 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_80'] = x_github_request__mutmut_80 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_81'] = x_github_request__mutmut_81 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_82'] = x_github_request__mutmut_82 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_83'] = x_github_request__mutmut_83 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_84'] = x_github_request__mutmut_84 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_85'] = x_github_request__mutmut_85 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_86'] = x_github_request__mutmut_86 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_87'] = x_github_request__mutmut_87 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_88'] = x_github_request__mutmut_88 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_89'] = x_github_request__mutmut_89 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_90'] = x_github_request__mutmut_90 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_91'] = x_github_request__mutmut_91 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_92'] = x_github_request__mutmut_92 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_93'] = x_github_request__mutmut_93 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_94'] = x_github_request__mutmut_94 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_95'] = x_github_request__mutmut_95 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_96'] = x_github_request__mutmut_96 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_97'] = x_github_request__mutmut_97 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_98'] = x_github_request__mutmut_98 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_99'] = x_github_request__mutmut_99 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_100'] = x_github_request__mutmut_100 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_101'] = x_github_request__mutmut_101 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_102'] = x_github_request__mutmut_102 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_103'] = x_github_request__mutmut_103 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_104'] = x_github_request__mutmut_104 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_105'] = x_github_request__mutmut_105 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_106'] = x_github_request__mutmut_106 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_107'] = x_github_request__mutmut_107 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_108'] = x_github_request__mutmut_108 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_109'] = x_github_request__mutmut_109 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_110'] = x_github_request__mutmut_110 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_111'] = x_github_request__mutmut_111 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_112'] = x_github_request__mutmut_112 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_113'] = x_github_request__mutmut_113 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_114'] = x_github_request__mutmut_114 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_115'] = x_github_request__mutmut_115 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_116'] = x_github_request__mutmut_116 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_117'] = x_github_request__mutmut_117 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_118'] = x_github_request__mutmut_118 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_119'] = x_github_request__mutmut_119 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_120'] = x_github_request__mutmut_120 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_121'] = x_github_request__mutmut_121 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_122'] = x_github_request__mutmut_122 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_123'] = x_github_request__mutmut_123 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_124'] = x_github_request__mutmut_124 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_125'] = x_github_request__mutmut_125 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_126'] = x_github_request__mutmut_126 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_127'] = x_github_request__mutmut_127 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_128'] = x_github_request__mutmut_128 # type: ignore # mutmut generated
mutants_x_github_request__mutmut['x_github_request__mutmut_129'] = x_github_request__mutmut_129 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_discover_repositories__mutmut)
def discover_repositories():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_orig():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_1():
    """Discover list of active repositories to monitor."""
    repos = None

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_2():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(None):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_3():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split("XX,XX"):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_4():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = None
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_5():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(None)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_6():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(None)

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_7():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(None))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_8():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER or ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_9():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = None
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_10():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) + timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_11():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(None) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_12():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=None)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_13():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = None
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_14():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 2
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_15():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while False:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_16():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = None
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_17():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(None)
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_18():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) and not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_19():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_20():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_21():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                return

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_22():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = None
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_23():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = True
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_24():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) and item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_25():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_26():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get(None, False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_27():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", None):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_28():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get(False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_29():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", ):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_30():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("XXarchivedXX", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_31():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("ARCHIVED", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_32():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", True):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_33():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    break

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_34():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = None
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_35():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get(None, "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_36():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", None)
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_37():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_38():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", )
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_39():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("XXfull_nameXX", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_40():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("FULL_NAME", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_41():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "XXXX")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_42():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER or not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_43():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_44():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(None):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_45():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    break

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_46():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = None
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_47():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get(None)
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_48():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("XXpushed_atXX")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_49():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("PUSHED_AT")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_50():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = None
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_51():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(None)
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_52():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace(None, "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_53():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", None))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_54():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_55():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", ))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_56():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("XXZXX", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_57():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_58():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "XX+00:00XX"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_59():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at <= cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_60():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = None
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_61():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = False
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_62():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            return
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_63():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(None)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_64():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination and len(data) < 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_65():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) <= 100:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_66():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 101:
                break
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_67():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                return
            page += 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_68():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page = 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_69():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page -= 1

    return sorted(list(repos))


def x_discover_repositories__mutmut_70():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 2

    return sorted(list(repos))


def x_discover_repositories__mutmut_71():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(None)


def x_discover_repositories__mutmut_72():
    """Discover list of active repositories to monitor."""
    repos = set()

    if REPOS_CONFIG:
        for r in REPOS_CONFIG.split(","):
            r = r.strip()
            if r:
                repos.add(r)
        return sorted(list(repos))

    if AUTO_DISCOVER and ACCESS_TOKEN:
        cutoff_date = datetime.now(timezone.utc) - timedelta(days=ACTIVE_DAYS)
        page = 1
        while True:
            data = github_request(f"/user/repos?per_page=100&affiliation=owner&sort=pushed&direction=desc&page={page}")
            if not isinstance(data, list) or not data:
                break

            stop_pagination = False
            for item in data:
                if not isinstance(item, dict) or item.get("archived", False):
                    continue

                full_name = item.get("full_name", "")
                if OWNER and not full_name.startswith(f"{OWNER}/"):
                    continue

                pushed_at_str = item.get("pushed_at")
                if pushed_at_str:
                    try:
                        pushed_at = datetime.fromisoformat(pushed_at_str.replace("Z", "+00:00"))
                        if pushed_at < cutoff_date:
                            stop_pagination = True
                            break
                    except Exception:
                        pass

                repos.add(full_name)

            if stop_pagination or len(data) < 100:
                break
            page += 1

    return sorted(list(None))

mutants_x_discover_repositories__mutmut['_mutmut_orig'] = x_discover_repositories__mutmut_orig # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_1'] = x_discover_repositories__mutmut_1 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_2'] = x_discover_repositories__mutmut_2 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_3'] = x_discover_repositories__mutmut_3 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_4'] = x_discover_repositories__mutmut_4 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_5'] = x_discover_repositories__mutmut_5 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_6'] = x_discover_repositories__mutmut_6 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_7'] = x_discover_repositories__mutmut_7 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_8'] = x_discover_repositories__mutmut_8 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_9'] = x_discover_repositories__mutmut_9 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_10'] = x_discover_repositories__mutmut_10 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_11'] = x_discover_repositories__mutmut_11 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_12'] = x_discover_repositories__mutmut_12 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_13'] = x_discover_repositories__mutmut_13 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_14'] = x_discover_repositories__mutmut_14 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_15'] = x_discover_repositories__mutmut_15 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_16'] = x_discover_repositories__mutmut_16 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_17'] = x_discover_repositories__mutmut_17 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_18'] = x_discover_repositories__mutmut_18 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_19'] = x_discover_repositories__mutmut_19 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_20'] = x_discover_repositories__mutmut_20 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_21'] = x_discover_repositories__mutmut_21 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_22'] = x_discover_repositories__mutmut_22 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_23'] = x_discover_repositories__mutmut_23 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_24'] = x_discover_repositories__mutmut_24 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_25'] = x_discover_repositories__mutmut_25 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_26'] = x_discover_repositories__mutmut_26 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_27'] = x_discover_repositories__mutmut_27 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_28'] = x_discover_repositories__mutmut_28 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_29'] = x_discover_repositories__mutmut_29 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_30'] = x_discover_repositories__mutmut_30 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_31'] = x_discover_repositories__mutmut_31 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_32'] = x_discover_repositories__mutmut_32 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_33'] = x_discover_repositories__mutmut_33 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_34'] = x_discover_repositories__mutmut_34 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_35'] = x_discover_repositories__mutmut_35 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_36'] = x_discover_repositories__mutmut_36 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_37'] = x_discover_repositories__mutmut_37 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_38'] = x_discover_repositories__mutmut_38 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_39'] = x_discover_repositories__mutmut_39 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_40'] = x_discover_repositories__mutmut_40 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_41'] = x_discover_repositories__mutmut_41 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_42'] = x_discover_repositories__mutmut_42 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_43'] = x_discover_repositories__mutmut_43 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_44'] = x_discover_repositories__mutmut_44 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_45'] = x_discover_repositories__mutmut_45 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_46'] = x_discover_repositories__mutmut_46 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_47'] = x_discover_repositories__mutmut_47 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_48'] = x_discover_repositories__mutmut_48 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_49'] = x_discover_repositories__mutmut_49 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_50'] = x_discover_repositories__mutmut_50 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_51'] = x_discover_repositories__mutmut_51 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_52'] = x_discover_repositories__mutmut_52 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_53'] = x_discover_repositories__mutmut_53 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_54'] = x_discover_repositories__mutmut_54 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_55'] = x_discover_repositories__mutmut_55 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_56'] = x_discover_repositories__mutmut_56 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_57'] = x_discover_repositories__mutmut_57 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_58'] = x_discover_repositories__mutmut_58 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_59'] = x_discover_repositories__mutmut_59 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_60'] = x_discover_repositories__mutmut_60 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_61'] = x_discover_repositories__mutmut_61 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_62'] = x_discover_repositories__mutmut_62 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_63'] = x_discover_repositories__mutmut_63 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_64'] = x_discover_repositories__mutmut_64 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_65'] = x_discover_repositories__mutmut_65 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_66'] = x_discover_repositories__mutmut_66 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_67'] = x_discover_repositories__mutmut_67 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_68'] = x_discover_repositories__mutmut_68 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_69'] = x_discover_repositories__mutmut_69 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_70'] = x_discover_repositories__mutmut_70 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_71'] = x_discover_repositories__mutmut_71 # type: ignore # mutmut generated
mutants_x_discover_repositories__mutmut['x_discover_repositories__mutmut_72'] = x_discover_repositories__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_queued_job_details__mutmut)
def get_queued_job_details(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_orig(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_1(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = None
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_2(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(None)
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_3(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data and "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_4(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_5(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "XXworkflow_runsXX" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_6(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "WORKFLOW_RUNS" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_7(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_8(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = None
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_9(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["XXworkflow_runsXX"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_10(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["WORKFLOW_RUNS"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_11(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = None
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_12(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(None)
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_13(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['XXidXX']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_14(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['ID']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_15(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data or "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_16(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "XXjobsXX" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_17(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "JOBS" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_18(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" not in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_19(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["XXjobsXX"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_20(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["JOBS"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_21(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get(None) == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_22(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("XXstatusXX") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_23(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("STATUS") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_24(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") != "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_25(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "XXqueuedXX":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_26(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "QUEUED":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_27(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = None
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_28(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.upper() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_29(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get(None, [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_30(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", None)]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_31(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get([])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_32(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", )]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_33(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("XXlabelsXX", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_34(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("LABELS", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_35(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append(None)
    return queued_jobs


def x_get_queued_job_details__mutmut_36(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "XXidXX": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_37(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "ID": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_38(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get(None),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_39(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("XXidXX"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_40(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("ID"),
                        "name": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_41(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "XXnameXX": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_42(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "NAME": job.get("name"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_43(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get(None),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_44(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("XXnameXX"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_45(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("NAME"),
                        "labels": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_46(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "XXlabelsXX": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_47(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "LABELS": labels,
                        "repo": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_48(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "XXrepoXX": repo_full_name
                    })
    return queued_jobs


def x_get_queued_job_details__mutmut_49(repo_full_name: str) -> List[Dict[str, Any]]:
    """Inspect queued workflow runs and extract job labels for hybrid routing."""
    data = github_request(f"/repos/{repo_full_name}/actions/runs?status=queued")
    if not data or "workflow_runs" not in data:
        return []

    queued_jobs = []
    for run in data["workflow_runs"]:
        jobs_data = github_request(f"/repos/{repo_full_name}/actions/runs/{run['id']}/jobs?filter=latest")
        if jobs_data and "jobs" in jobs_data:
            for job in jobs_data["jobs"]:
                if job.get("status") == "queued":
                    labels = [lbl.lower() for lbl in job.get("labels", [])]
                    queued_jobs.append({
                        "id": job.get("id"),
                        "name": job.get("name"),
                        "labels": labels,
                        "REPO": repo_full_name
                    })
    return queued_jobs

mutants_x_get_queued_job_details__mutmut['_mutmut_orig'] = x_get_queued_job_details__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_1'] = x_get_queued_job_details__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_2'] = x_get_queued_job_details__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_3'] = x_get_queued_job_details__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_4'] = x_get_queued_job_details__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_5'] = x_get_queued_job_details__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_6'] = x_get_queued_job_details__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_7'] = x_get_queued_job_details__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_8'] = x_get_queued_job_details__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_9'] = x_get_queued_job_details__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_10'] = x_get_queued_job_details__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_11'] = x_get_queued_job_details__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_12'] = x_get_queued_job_details__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_13'] = x_get_queued_job_details__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_14'] = x_get_queued_job_details__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_15'] = x_get_queued_job_details__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_16'] = x_get_queued_job_details__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_17'] = x_get_queued_job_details__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_18'] = x_get_queued_job_details__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_19'] = x_get_queued_job_details__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_20'] = x_get_queued_job_details__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_21'] = x_get_queued_job_details__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_22'] = x_get_queued_job_details__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_23'] = x_get_queued_job_details__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_24'] = x_get_queued_job_details__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_25'] = x_get_queued_job_details__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_26'] = x_get_queued_job_details__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_27'] = x_get_queued_job_details__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_28'] = x_get_queued_job_details__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_29'] = x_get_queued_job_details__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_30'] = x_get_queued_job_details__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_31'] = x_get_queued_job_details__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_32'] = x_get_queued_job_details__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_33'] = x_get_queued_job_details__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_34'] = x_get_queued_job_details__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_35'] = x_get_queued_job_details__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_36'] = x_get_queued_job_details__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_37'] = x_get_queued_job_details__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_38'] = x_get_queued_job_details__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_39'] = x_get_queued_job_details__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_40'] = x_get_queued_job_details__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_41'] = x_get_queued_job_details__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_42'] = x_get_queued_job_details__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_43'] = x_get_queued_job_details__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_44'] = x_get_queued_job_details__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_45'] = x_get_queued_job_details__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_46'] = x_get_queued_job_details__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_47'] = x_get_queued_job_details__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_48'] = x_get_queued_job_details__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_queued_job_details__mutmut['x_get_queued_job_details__mutmut_49'] = x_get_queued_job_details__mutmut_49 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_select_driver_for_job__mutmut)
def select_driver_for_job(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_orig(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_1(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = None

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_2(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get(None, [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_3(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", None)

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_4(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get([])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_5(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", )

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_6(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("XXlabelsXX", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_7(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("LABELS", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_8(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = None

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_9(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(None)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_10(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger not in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_11(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm or AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_12(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("XXorbstack-vmXX", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_13(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("ORBSTACK-VM", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_14(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "XXwsl2XX", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_15(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "WSL2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_16(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "XXmultipassXX"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_17(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "MULTIPASS"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_18(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name not in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_19(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "XXvmXX"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_20(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "VM"

    return default_driver, "container"


def x_select_driver_for_job__mutmut_21(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "XXcontainerXX"


def x_select_driver_for_job__mutmut_22(
    job: Dict[str, Any],
    default_driver: RunnerDriver,
    available_drivers: Dict[str, RunnerDriver]
) -> Tuple[RunnerDriver, str]:
    """Determine whether a job requires a VM driver or a standard container driver."""
    job_labels = job.get("labels", [])

    # Check if job specifically requests a VM or features requiring full OS sandbox
    needs_vm = any(trigger in job_labels for trigger in VM_TRIGGER_LABELS)

    if needs_vm and AUTO_ROUTE_VM:
        # Prefer VM backends: orbstack-vm -> wsl2 -> multipass
        for vm_name in ("orbstack-vm", "wsl2", "multipass"):
            if vm_name in available_drivers:
                return available_drivers[vm_name], "vm"

    return default_driver, "CONTAINER"

mutants_x_select_driver_for_job__mutmut['_mutmut_orig'] = x_select_driver_for_job__mutmut_orig # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_1'] = x_select_driver_for_job__mutmut_1 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_2'] = x_select_driver_for_job__mutmut_2 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_3'] = x_select_driver_for_job__mutmut_3 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_4'] = x_select_driver_for_job__mutmut_4 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_5'] = x_select_driver_for_job__mutmut_5 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_6'] = x_select_driver_for_job__mutmut_6 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_7'] = x_select_driver_for_job__mutmut_7 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_8'] = x_select_driver_for_job__mutmut_8 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_9'] = x_select_driver_for_job__mutmut_9 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_10'] = x_select_driver_for_job__mutmut_10 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_11'] = x_select_driver_for_job__mutmut_11 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_12'] = x_select_driver_for_job__mutmut_12 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_13'] = x_select_driver_for_job__mutmut_13 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_14'] = x_select_driver_for_job__mutmut_14 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_15'] = x_select_driver_for_job__mutmut_15 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_16'] = x_select_driver_for_job__mutmut_16 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_17'] = x_select_driver_for_job__mutmut_17 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_18'] = x_select_driver_for_job__mutmut_18 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_19'] = x_select_driver_for_job__mutmut_19 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_20'] = x_select_driver_for_job__mutmut_20 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_21'] = x_select_driver_for_job__mutmut_21 # type: ignore # mutmut generated
mutants_x_select_driver_for_job__mutmut['x_select_driver_for_job__mutmut_22'] = x_select_driver_for_job__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_target_architectures__mutmut)
def get_target_architectures():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_orig():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_1():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH not in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_2():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("XXbothXX", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_3():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("BOTH", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_4():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "XXallXX", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_5():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "ALL", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_6():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "XXdualXX"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_7():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "DUAL"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_8():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["XXarm64XX", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_9():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["ARM64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_10():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "XXamd64XX"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_11():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "AMD64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_12():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH not in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_13():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("XXamd64XX", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_14():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("AMD64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_15():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "XXx86_64XX", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_16():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "X86_64", "x64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_17():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "XXx64XX"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_18():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "X64"):
        return ["amd64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_19():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["XXamd64XX"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_20():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["AMD64"]
    else:
        return ["arm64"]


def x_get_target_architectures__mutmut_21():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["XXarm64XX"]


def x_get_target_architectures__mutmut_22():
    """Return list of architectures to spin up."""
    if RUNNER_ARCH in ("both", "all", "dual"):
        return ["arm64", "amd64"]
    elif RUNNER_ARCH in ("amd64", "x86_64", "x64"):
        return ["amd64"]
    else:
        return ["ARM64"]

mutants_x_get_target_architectures__mutmut['_mutmut_orig'] = x_get_target_architectures__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_1'] = x_get_target_architectures__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_2'] = x_get_target_architectures__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_3'] = x_get_target_architectures__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_4'] = x_get_target_architectures__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_5'] = x_get_target_architectures__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_6'] = x_get_target_architectures__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_7'] = x_get_target_architectures__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_8'] = x_get_target_architectures__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_9'] = x_get_target_architectures__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_10'] = x_get_target_architectures__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_11'] = x_get_target_architectures__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_12'] = x_get_target_architectures__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_13'] = x_get_target_architectures__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_14'] = x_get_target_architectures__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_15'] = x_get_target_architectures__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_16'] = x_get_target_architectures__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_17'] = x_get_target_architectures__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_18'] = x_get_target_architectures__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_19'] = x_get_target_architectures__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_20'] = x_get_target_architectures__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_21'] = x_get_target_architectures__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_target_architectures__mutmut['x_get_target_architectures__mutmut_22'] = x_get_target_architectures__mutmut_22 # type: ignore # mutmut generated
mutants_x_main__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_main__mutmut)
def main():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_orig():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_1():
    if ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_2():
    if not ACCESS_TOKEN:
        print(None, file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_3():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=None)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_4():
    if not ACCESS_TOKEN:
        print(file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_5():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", )
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_6():
    if not ACCESS_TOKEN:
        print("XX[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.XX", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_7():
    if not ACCESS_TOKEN:
        print("[autoscaler] error: access_token is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_8():
    if not ACCESS_TOKEN:
        print("[AUTOSCALER] ERROR: ACCESS_TOKEN IS REQUIRED FOR AUTOSCALING.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_9():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(None)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_10():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(2)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_11():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = None
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_12():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = None
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_13():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(None)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_14():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = None
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_15():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = None

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_16():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print(None)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_17():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" / 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_18():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("XX=XX" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_19():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 66)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_20():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(None)
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_21():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print("XX ⚡ RunZero — Dual-Engine Local GitHub Runner AutoscalerXX")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_22():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ runzero — dual-engine local github runner autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_23():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RUNZERO — DUAL-ENGINE LOCAL GITHUB RUNNER AUTOSCALER")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_24():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(None)
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_25():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().lower()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_26():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(None)
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_27():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join(None)}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_28():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {'XX, XX'.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_29():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.lower() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_30():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(None)
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_31():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'XXEnabled (Auto-detecting VM vs Container jobs)XX' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_32():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'enabled (auto-detecting vm vs container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_33():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'ENABLED (AUTO-DETECTING VM VS CONTAINER JOBS)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_34():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'XXDisabledXX'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_35():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_36():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'DISABLED'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_37():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(None)
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_38():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join(None)}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_39():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {'XX, XX'.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_40():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.lower() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_41():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(None)
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_42():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'XXEnabledXX' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_43():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_44():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'ENABLED' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_45():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'XXDisabledXX'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_46():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_47():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'DISABLED'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_48():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(None)
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_49():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(None)
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_50():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print(None)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_51():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" / 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_52():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("XX=XX" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_53():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 66)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_54():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = None
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_55():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 1
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_56():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = None

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_57():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = None
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_58():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG or (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_59():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_60():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL and not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_61():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now + last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_62():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time >= DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_63():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_64():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = None
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_65():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = None
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_66():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = None
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_67():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(None)
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_68():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(None)
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_69():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(None)

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_70():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = None
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_71():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = None
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_72():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(None)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_73():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(None)

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_74():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = None
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_75():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state != "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_76():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "XXrunningXX"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_77():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "RUNNING"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_78():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = None

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_79():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS or active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_80():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count <= MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_81():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count <= MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_82():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = None
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_83():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(None, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_84():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, None)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_85():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_86():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, )
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_87():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS + active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_88():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS + active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_89():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(None):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_90():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = None
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_91():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i / len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_92():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=None,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_93():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=None,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_94():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=None,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_95():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=None,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_96():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=None
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_97():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_98():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_99():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_100():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_101():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_102():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = None
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_103():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = None

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_104():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 1

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_105():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = None
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_106():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(None)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_107():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = None
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_108():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued = len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_109():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued -= len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_110():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(None)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_111():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(1.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_112():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued >= 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_113():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 1:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_114():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(None)

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_115():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = None
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_116():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(None)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_117():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(2 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_118():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo != repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_119():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = None

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_120():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) + active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_121():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS and needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_122():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) > MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_123():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed < 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_124():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 1:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_125():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        return

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_126():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = None
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_127():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(None, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_128():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, None, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_129():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, None)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_130():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_131():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_132():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, )
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_133():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = None
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_134():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[1]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_135():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) and "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_136():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "XXamd64XX" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_137():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "AMD64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_138():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" not in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_139():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get(None, []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_140():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", None) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_141():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get([]) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_142():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", ) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_143():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("XXlabelsXX", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_144():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("LABELS", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_145():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "XXx64XX" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_146():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "X64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_147():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" not in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_148():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get(None, []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_149():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", None):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_150():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get([]):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_151():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", ):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_152():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("XXlabelsXX", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_153():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("LABELS", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_154():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = None

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_155():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "XXamd64XX"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_156():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "AMD64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_157():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = None
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_158():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=None,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_159():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=None,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_160():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=None,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_161():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=None,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_162():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=None
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_163():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_164():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_165():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_166():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_167():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_168():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(None)
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_169():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=None,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_170():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=None,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_171():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status=None,
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_172():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state=None,
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_173():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=None,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_174():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=None,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_175():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=None
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_176():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_177():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_178():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_179():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_180():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_181():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_182():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_183():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="XXrunningXX",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_184():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="RUNNING",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_185():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="XXrunningXX",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_186():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="RUNNING",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_187():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed = 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_188():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed += 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_189():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 2

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_190():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS or tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_191():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS or active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_192():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count <= MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_193():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count <= MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_194():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = None
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_195():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(None, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_196():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, None)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_197():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_198():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, )
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_199():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS + active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_200():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS + active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_201():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(None):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_202():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = None
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_203():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i / len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_204():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=None,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_205():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=None,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_206():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=None,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_207():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=None,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_208():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=None
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_209():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_210():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_211():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_212():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_213():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_214():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[1],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_215():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = None
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_216():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(None, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_217():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, None)
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_218():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_219():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, )
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_220():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(None))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_221():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) / 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_222():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 1.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_223():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(None)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_224():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print(None)
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_225():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("XX[Autoscaler] Stopping managed runners on shutdown...XX")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_226():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[autoscaler] stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_227():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[AUTOSCALER] STOPPING MANAGED RUNNERS ON SHUTDOWN...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[Autoscaler] Shutdown complete.")


def x_main__mutmut_228():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print(None)


def x_main__mutmut_229():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("XX[Autoscaler] Shutdown complete.XX")


def x_main__mutmut_230():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[autoscaler] shutdown complete.")


def x_main__mutmut_231():
    if not ACCESS_TOKEN:
        print("[Autoscaler] Error: ACCESS_TOKEN is required for autoscaling.", file=sys.stderr)
        sys.exit(1)

    available_drivers = get_available_drivers()
    default_driver = get_driver(RUNNER_BACKEND)
    architectures = get_target_architectures()
    cache_mounts = init_cache_dirs()

    print("=" * 65)
    print(" ⚡ RunZero — Dual-Engine Local GitHub Runner Autoscaler")
    print(f" Default Engine:   {default_driver.name().upper()}")
    print(f" Available Drivers: {', '.join([k.upper() for k in available_drivers.keys()])}")
    print(f" Hybrid Routing:   {'Enabled (Auto-detecting VM vs Container jobs)' if AUTO_ROUTE_VM else 'Disabled'}")
    print(f" Architectures:    {', '.join([a.upper() for a in architectures])}")
    print(f" Cache Directory:  {HOST_CACHE_DIR} ({'Enabled' if CACHE_ENABLED else 'Disabled'})")
    print(f" Max Concurrency:  {MAX_RUNNERS} | Min Runners: {MIN_RUNNERS}")
    print(f" Active Filter:    Pushed within last {ACTIVE_DAYS} days")
    print("=" * 65)

    last_discovery_time = 0
    tracked_repos = []

    while running:
        now = time.time()
        # Refresh tracked repositories periodically
        if not ORG and (now - last_discovery_time > DISCOVERY_INTERVAL or not tracked_repos):
            discovered = discover_repositories()
            last_discovery_time = now
            if discovered:
                tracked_repos = discovered
                print(f"[Autoscaler] Monitoring {len(tracked_repos)} active repository(ies):")
                for r in tracked_repos:
                    print(f"  • {r}")
                print(f"[Autoscaler] GitHub API Quota remaining: {rate_limit_remaining}/5000")

        # Collect active runners across all drivers
        all_runners = []
        for d in available_drivers.values():
            runners = d.list_runners()
            d.prune_exited(runners)
            all_runners.extend(d.list_runners())

        active_runners = [r for r in all_runners if r.state == "running"]
        active_count = len(active_runners)

        if ORG:
            # For organizations
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        org=ORG,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
        else:
            # For repositories: check queued jobs with hybrid routing
            queued_jobs_by_repo = {}
            total_queued = 0

            for repo in tracked_repos:
                jobs = get_queued_job_details(repo)
                if jobs:
                    queued_jobs_by_repo[repo] = jobs
                    total_queued += len(jobs)
                time.sleep(0.1)

            if total_queued > 0:
                print(f"[Autoscaler] Detected {total_queued} queued unclaimed job(s) across repos.")

            for repo, jobs in queued_jobs_by_repo.items():
                active_for_repo = sum(1 for r in active_runners if r.target_repo == repo)
                needed = len(jobs) - active_for_repo

                for job in jobs:
                    if len(active_runners) >= MAX_RUNNERS or needed <= 0:
                        break

                    driver_to_use, mode = select_driver_for_job(job, default_driver, available_drivers)
                    arch = architectures[0]
                    if "amd64" in job.get("labels", []) or "x64" in job.get("labels", []):
                        arch = "amd64"

                    spawned_id = driver_to_use.spawn_runner(
                        repo=repo,
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )
                    if spawned_id:
                        active_runners.append(RunnerInfo(
                            id=spawned_id,
                            name=spawned_id,
                            status="running",
                            state="running",
                            target_repo=repo,
                            target_arch=arch,
                            backend=driver_to_use.name()
                        ))
                        needed -= 1

            # Maintain MIN_RUNNERS if configured
            if active_count < MIN_RUNNERS and active_count < MAX_RUNNERS and tracked_repos:
                needed = min(MIN_RUNNERS - active_count, MAX_RUNNERS - active_count)
                for i in range(needed):
                    arch = architectures[i % len(architectures)]
                    default_driver.spawn_runner(
                        repo=tracked_repos[0],
                        arch=arch,
                        access_token=ACCESS_TOKEN,
                        cache_mounts=cache_mounts,
                        proxies_enabled=PROXIES_ENABLED
                    )

        # Adaptive sleep pacing
        sleep_duration = max(POLL_INTERVAL, int(len(tracked_repos) * 0.5))
        time.sleep(sleep_duration)

    # Cleanup remaining runners across all drivers on shutdown
    print("[Autoscaler] Stopping managed runners on shutdown...")
    for d in available_drivers.values():
        d.cleanup_all()
    print("[AUTOSCALER] SHUTDOWN COMPLETE.")

mutants_x_main__mutmut['_mutmut_orig'] = x_main__mutmut_orig # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_1'] = x_main__mutmut_1 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_2'] = x_main__mutmut_2 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_3'] = x_main__mutmut_3 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_4'] = x_main__mutmut_4 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_5'] = x_main__mutmut_5 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_6'] = x_main__mutmut_6 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_7'] = x_main__mutmut_7 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_8'] = x_main__mutmut_8 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_9'] = x_main__mutmut_9 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_10'] = x_main__mutmut_10 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_11'] = x_main__mutmut_11 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_12'] = x_main__mutmut_12 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_13'] = x_main__mutmut_13 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_14'] = x_main__mutmut_14 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_15'] = x_main__mutmut_15 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_16'] = x_main__mutmut_16 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_17'] = x_main__mutmut_17 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_18'] = x_main__mutmut_18 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_19'] = x_main__mutmut_19 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_20'] = x_main__mutmut_20 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_21'] = x_main__mutmut_21 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_22'] = x_main__mutmut_22 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_23'] = x_main__mutmut_23 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_24'] = x_main__mutmut_24 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_25'] = x_main__mutmut_25 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_26'] = x_main__mutmut_26 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_27'] = x_main__mutmut_27 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_28'] = x_main__mutmut_28 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_29'] = x_main__mutmut_29 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_30'] = x_main__mutmut_30 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_31'] = x_main__mutmut_31 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_32'] = x_main__mutmut_32 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_33'] = x_main__mutmut_33 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_34'] = x_main__mutmut_34 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_35'] = x_main__mutmut_35 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_36'] = x_main__mutmut_36 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_37'] = x_main__mutmut_37 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_38'] = x_main__mutmut_38 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_39'] = x_main__mutmut_39 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_40'] = x_main__mutmut_40 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_41'] = x_main__mutmut_41 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_42'] = x_main__mutmut_42 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_43'] = x_main__mutmut_43 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_44'] = x_main__mutmut_44 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_45'] = x_main__mutmut_45 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_46'] = x_main__mutmut_46 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_47'] = x_main__mutmut_47 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_48'] = x_main__mutmut_48 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_49'] = x_main__mutmut_49 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_50'] = x_main__mutmut_50 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_51'] = x_main__mutmut_51 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_52'] = x_main__mutmut_52 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_53'] = x_main__mutmut_53 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_54'] = x_main__mutmut_54 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_55'] = x_main__mutmut_55 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_56'] = x_main__mutmut_56 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_57'] = x_main__mutmut_57 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_58'] = x_main__mutmut_58 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_59'] = x_main__mutmut_59 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_60'] = x_main__mutmut_60 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_61'] = x_main__mutmut_61 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_62'] = x_main__mutmut_62 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_63'] = x_main__mutmut_63 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_64'] = x_main__mutmut_64 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_65'] = x_main__mutmut_65 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_66'] = x_main__mutmut_66 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_67'] = x_main__mutmut_67 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_68'] = x_main__mutmut_68 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_69'] = x_main__mutmut_69 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_70'] = x_main__mutmut_70 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_71'] = x_main__mutmut_71 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_72'] = x_main__mutmut_72 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_73'] = x_main__mutmut_73 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_74'] = x_main__mutmut_74 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_75'] = x_main__mutmut_75 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_76'] = x_main__mutmut_76 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_77'] = x_main__mutmut_77 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_78'] = x_main__mutmut_78 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_79'] = x_main__mutmut_79 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_80'] = x_main__mutmut_80 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_81'] = x_main__mutmut_81 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_82'] = x_main__mutmut_82 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_83'] = x_main__mutmut_83 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_84'] = x_main__mutmut_84 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_85'] = x_main__mutmut_85 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_86'] = x_main__mutmut_86 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_87'] = x_main__mutmut_87 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_88'] = x_main__mutmut_88 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_89'] = x_main__mutmut_89 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_90'] = x_main__mutmut_90 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_91'] = x_main__mutmut_91 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_92'] = x_main__mutmut_92 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_93'] = x_main__mutmut_93 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_94'] = x_main__mutmut_94 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_95'] = x_main__mutmut_95 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_96'] = x_main__mutmut_96 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_97'] = x_main__mutmut_97 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_98'] = x_main__mutmut_98 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_99'] = x_main__mutmut_99 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_100'] = x_main__mutmut_100 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_101'] = x_main__mutmut_101 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_102'] = x_main__mutmut_102 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_103'] = x_main__mutmut_103 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_104'] = x_main__mutmut_104 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_105'] = x_main__mutmut_105 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_106'] = x_main__mutmut_106 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_107'] = x_main__mutmut_107 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_108'] = x_main__mutmut_108 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_109'] = x_main__mutmut_109 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_110'] = x_main__mutmut_110 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_111'] = x_main__mutmut_111 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_112'] = x_main__mutmut_112 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_113'] = x_main__mutmut_113 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_114'] = x_main__mutmut_114 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_115'] = x_main__mutmut_115 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_116'] = x_main__mutmut_116 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_117'] = x_main__mutmut_117 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_118'] = x_main__mutmut_118 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_119'] = x_main__mutmut_119 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_120'] = x_main__mutmut_120 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_121'] = x_main__mutmut_121 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_122'] = x_main__mutmut_122 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_123'] = x_main__mutmut_123 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_124'] = x_main__mutmut_124 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_125'] = x_main__mutmut_125 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_126'] = x_main__mutmut_126 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_127'] = x_main__mutmut_127 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_128'] = x_main__mutmut_128 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_129'] = x_main__mutmut_129 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_130'] = x_main__mutmut_130 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_131'] = x_main__mutmut_131 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_132'] = x_main__mutmut_132 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_133'] = x_main__mutmut_133 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_134'] = x_main__mutmut_134 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_135'] = x_main__mutmut_135 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_136'] = x_main__mutmut_136 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_137'] = x_main__mutmut_137 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_138'] = x_main__mutmut_138 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_139'] = x_main__mutmut_139 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_140'] = x_main__mutmut_140 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_141'] = x_main__mutmut_141 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_142'] = x_main__mutmut_142 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_143'] = x_main__mutmut_143 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_144'] = x_main__mutmut_144 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_145'] = x_main__mutmut_145 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_146'] = x_main__mutmut_146 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_147'] = x_main__mutmut_147 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_148'] = x_main__mutmut_148 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_149'] = x_main__mutmut_149 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_150'] = x_main__mutmut_150 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_151'] = x_main__mutmut_151 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_152'] = x_main__mutmut_152 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_153'] = x_main__mutmut_153 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_154'] = x_main__mutmut_154 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_155'] = x_main__mutmut_155 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_156'] = x_main__mutmut_156 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_157'] = x_main__mutmut_157 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_158'] = x_main__mutmut_158 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_159'] = x_main__mutmut_159 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_160'] = x_main__mutmut_160 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_161'] = x_main__mutmut_161 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_162'] = x_main__mutmut_162 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_163'] = x_main__mutmut_163 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_164'] = x_main__mutmut_164 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_165'] = x_main__mutmut_165 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_166'] = x_main__mutmut_166 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_167'] = x_main__mutmut_167 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_168'] = x_main__mutmut_168 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_169'] = x_main__mutmut_169 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_170'] = x_main__mutmut_170 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_171'] = x_main__mutmut_171 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_172'] = x_main__mutmut_172 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_173'] = x_main__mutmut_173 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_174'] = x_main__mutmut_174 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_175'] = x_main__mutmut_175 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_176'] = x_main__mutmut_176 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_177'] = x_main__mutmut_177 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_178'] = x_main__mutmut_178 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_179'] = x_main__mutmut_179 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_180'] = x_main__mutmut_180 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_181'] = x_main__mutmut_181 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_182'] = x_main__mutmut_182 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_183'] = x_main__mutmut_183 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_184'] = x_main__mutmut_184 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_185'] = x_main__mutmut_185 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_186'] = x_main__mutmut_186 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_187'] = x_main__mutmut_187 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_188'] = x_main__mutmut_188 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_189'] = x_main__mutmut_189 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_190'] = x_main__mutmut_190 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_191'] = x_main__mutmut_191 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_192'] = x_main__mutmut_192 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_193'] = x_main__mutmut_193 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_194'] = x_main__mutmut_194 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_195'] = x_main__mutmut_195 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_196'] = x_main__mutmut_196 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_197'] = x_main__mutmut_197 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_198'] = x_main__mutmut_198 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_199'] = x_main__mutmut_199 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_200'] = x_main__mutmut_200 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_201'] = x_main__mutmut_201 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_202'] = x_main__mutmut_202 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_203'] = x_main__mutmut_203 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_204'] = x_main__mutmut_204 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_205'] = x_main__mutmut_205 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_206'] = x_main__mutmut_206 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_207'] = x_main__mutmut_207 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_208'] = x_main__mutmut_208 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_209'] = x_main__mutmut_209 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_210'] = x_main__mutmut_210 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_211'] = x_main__mutmut_211 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_212'] = x_main__mutmut_212 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_213'] = x_main__mutmut_213 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_214'] = x_main__mutmut_214 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_215'] = x_main__mutmut_215 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_216'] = x_main__mutmut_216 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_217'] = x_main__mutmut_217 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_218'] = x_main__mutmut_218 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_219'] = x_main__mutmut_219 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_220'] = x_main__mutmut_220 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_221'] = x_main__mutmut_221 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_222'] = x_main__mutmut_222 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_223'] = x_main__mutmut_223 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_224'] = x_main__mutmut_224 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_225'] = x_main__mutmut_225 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_226'] = x_main__mutmut_226 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_227'] = x_main__mutmut_227 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_228'] = x_main__mutmut_228 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_229'] = x_main__mutmut_229 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_230'] = x_main__mutmut_230 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_231'] = x_main__mutmut_231 # type: ignore # mutmut generated


if __name__ == "__main__":
    main()
